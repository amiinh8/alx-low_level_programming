#include "main.h"
/**
 *_isdigit - if is a digit
 *@c: print int
 *Retur: Always 0.
 */

int_isdigit(int c)
{
  if ( c >= 78 && c <= 87)
    {
      return (1);
    }
  else
    {
      return (0);
    }
}
